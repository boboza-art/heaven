import 'package:flutter/material.dart';

/// Haven design system colors.
///
/// Calm, warm, non-clinical palette.
class HavenColors {
  HavenColors._();

  /// Primary - warm, calming green
  static const Color primary = Color(0xFF6B9080);

  /// Secondary - soft lavender
  static const Color secondary = Color(0xFFB5C0D0);

  /// Surface - warm white
  static const Color surface = Color(0xFFFEFAF6);

  /// Background - off-white
  static const Color background = Color(0xFFFAF7F0);

  /// Text primary
  static const Color textPrimary = Color(0xFF2C2C2C);

  /// Text secondary
  static const Color textSecondary = Color(0xFF8E8E93);

  /// Mood colors
  static const Color moodGreat = Color(0xFF6B9080);
  static const Color moodGood = Color(0xFFA3B18A);
  static const Color moodOkay = Color(0xFFD4A373);
  static const Color moodLow = Color(0xFFE07A5F);
  static const Color moodBad = Color(0xFFD64045);
}
